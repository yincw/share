import CodeBlock from '@theme/CodeBlock';
import XYZ from "@site/src/examples/source/xyz.tsx";

# XYZ 源

<CodeBlock>
    <XYZ />
</CodeBlock>

```jsx showLineNumbers
// XYZ
import React, { JSX } from "react";
import { RMap, RLayerTile } from "rlayers";
import { fromLonLat } from "ol/proj";
import "ol/ol.css";

const origin = [114.322000, 30.375699];

const XYZ = (): JSX.Element => {
  const initial = {
    center: fromLonLat(origin),
    zoom: 11
  };
  // highlight-next-line
  const url = "http://webrd0{1-4}.is.autonavi.com/appmaptile?lang=zh_cn&size=1&scale=1&style=8&x={x}&y={y}&z={z}";

  return (
    <RMap
        width={"100%"}
        height={"60vh"}
        initial={initial}
    >
        // highlight-start
        <RLayerTile
            url={url}
            zIndex={0}
        />
        // highlight-end
    </RMap>
  );
}

export default XYZ;
```